# Local Dataset Mount Point

Active datasets are intentionally not tracked in git. Put or sync them here:

```text
data/Ghibli/
data/persona_5/
data/EVA_rei/
```

Each image should have a same-stem `.txt` caption. See `docs/datasets.md` for the expected format and audit checklist.

